export function ignoredJavaAstField(key) {
  return key === '_type'
    || key === 'type'
    || key === 'kind'
    || key === 'nodeType'
    || key === 'astKind'
    || key === 'treeKind'
    || key === 'nodeKind'
    || key === 'parent'
    || key === 'parentKind'
    || key === 'parentField'
    || key === 'binding'
    || key === 'resolvedBinding'
    || key === 'symbol'
    || key === 'scope'
    || key === 'range'
    || key === 'loc'
    || key === 'location'
    || key === 'pos'
    || key === 'end'
    || key === 'start'
    || key === 'endPosition'
    || key === 'startPosition'
    || key === 'length'
    || key === 'name'
    || key === 'identifier'
    || key === 'simpleName'
    || key === 'qualifiedName';
}
