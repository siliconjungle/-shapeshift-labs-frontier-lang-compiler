export function ignoredCSharpRoslynField(key) {
  return key === '_type'
    || key === 'type'
    || key === 'kind'
    || key === 'Kind'
    || key === 'nodeType'
    || key === 'syntaxKind'
    || key === 'SyntaxKind'
    || key === 'rawKind'
    || key === 'RawKind'
    || key === 'parent'
    || key === 'parentKind'
    || key === 'parentField'
    || key === 'span'
    || key === 'Span'
    || key === 'fullSpan'
    || key === 'FullSpan'
    || key === 'lineSpan'
    || key === 'location'
    || key === 'locations'
    || key === 'identifier'
    || key === 'name'
    || key === 'simpleName'
    || key === 'qualifiedName'
    || key === 'semanticModel'
    || key === 'symbol'
    || key === 'declaredSymbol'
    || key === 'typeInfo'
    || key === 'conversion'
    || key === 'constantValue';
}
