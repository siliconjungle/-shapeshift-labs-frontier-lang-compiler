export function ignoredKotlinPsiField(key) {
  return key === '_type'
    || key === 'type'
    || key === 'kind'
    || key === 'nodeType'
    || key === 'elementType'
    || key === 'psiKind'
    || key === 'parent'
    || key === 'parentKind'
    || key === 'parentField'
    || key === 'textRange'
    || key === 'range'
    || key === 'span'
    || key === 'location'
    || key === 'name'
    || key === 'nameIdentifier'
    || key === 'identifier'
    || key === 'fqName'
    || key === 'packageFqName'
    || key === 'analysisSymbol'
    || key === 'symbol'
    || key === 'typeInfo'
    || key === 'bindingContext'
    || key === 'fir'
    || key === 'ir';
}
