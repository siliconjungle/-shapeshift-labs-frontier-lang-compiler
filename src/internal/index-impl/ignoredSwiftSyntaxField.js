export function ignoredSwiftSyntaxField(key) {
  return key === '_type'
    || key === 'type'
    || key === 'kind'
    || key === 'syntaxKind'
    || key === 'SyntaxKind'
    || key === '_syntaxKind'
    || key === 'nodeType'
    || key === 'parent'
    || key === 'parentKind'
    || key === 'parentField'
    || key === 'position'
    || key === 'absolutePosition'
    || key === 'endPosition'
    || key === 'location'
    || key === 'sourceRange'
    || key === 'range'
    || key === 'span'
    || key === 'identifier'
    || key === 'name'
    || key === 'simpleName'
    || key === 'typeName'
    || key === 'sourceKitSymbol'
    || key === 'semanticModel'
    || key === 'resolvedSymbol'
    || key === 'typeInfo';
}
